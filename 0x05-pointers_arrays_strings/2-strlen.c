#include<string.h>
#include "main.h"
#include<stdio.h>

/**
 * _strlen - returns length of a str
 *
 *Return: length of str
 *@s: parameter s
 */

int _strlen(char *s)
{

	int len;

	len = strlen(s);

	return (len);

}
