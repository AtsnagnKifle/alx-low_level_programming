#include<string.h>
#include "main.h"

/**
 * _strcat - it concatenates two strings
 *
 * Return: return a string
 *@dest: parameter
 *@src: parameter
 */

char *_strcat(char *dest, char *src)
{

	strcat(dest,src);

	return (dest);
}
